[package]
name = "chromors-backend-vips"
version = "0.1.0"
edition = "2024"

[dependencies]
chromors-core = { path = "../chromors-core" }
libc = "0.2"
bytemuck = "1.25.0"

[build-dependencies]
bindgen = "0.71"
pkg-config = "0.3"

[workspace]
